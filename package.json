{
  "name": "cistour-price-monitor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MCP plugin and HTTP service for monitoring a Cistour excursion price.",
  "scripts": {
    "start": "node src/server.js",
    "daily": "node src/daily.js",
    "test": "node --test",
    "check": "node --check src/core.js && node --check src/cistour.js && node --check src/email.js && node --check src/server.js && node --check src/daily.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.5",
    "express": "^5.1.0",
    "zod": "^3.25.76"
  }
}
